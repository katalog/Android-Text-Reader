# 추가 아이디어 (백로그)

기본 기능 구현은 완료됨. 아래는 이후에 추가로 고려 중인 항목들.

- 공식 프로젝트 문카타 리더 만들고 그 공식 프로젝트에 링크로 apk, vscode, go서버 연결        

- ~~폴더 싱크 동작 이상함.~~
    ~~apk - 서버 간 싱크할때 연결 된 이후 apk 쪽에서 파일 다 받음. 그 이후에 서버쪽에서 파일 위치 변경(./d 폴더 를 ./c/d 로 옮기는 등) or
    폴더+파일 삭제, 빈 폴더 생성 등 했더니 apk 쪽에선 에러 남. 확인 필요~~
    → 2026-09-04에 여러 원인을 찾아 수정: (1) go 서버의 `/list`가 순회 도중 항목 하나가 사라지면(경합)
    스캔 전체를 실패시키던 문제 수정(go-moonkata-reader-sync-server의 filelist.go), (2) 안드로이드
    쪽 `PcSyncFileManager.sync()`가 SAF 예외 하나로 전체 동기화를 죽이지 않게 파일 단위로 방어,
    (3) 폴더 이동/삭제 뒤 로컬에 남는 빈 폴더 껍데기를 자동 정리, (4) 크기는 같지만 마지막 동기화 이후
    내용만 바뀐 파일을 놓치던 문제를 마지막 동기화 완료 시각 비교로 보완(`PcSyncFileManager.kt`).
    빈 폴더 자체는(원격에 파일이 하나도 없으니) 여전히 동기화 대상이 아님 — 의도된 동작.

- ~~챕터 건너뛰기 기능이 챕터패턴이 너무 가까운 경우 그냥 건너뛰게. 꼭 이렇게 연결되지 않더라도 챕터패턴 이후에 한 10줄? 20줄 안에 또 챕터패턴 있으면 그건 나누기 점프 하지말고 챕터패턴 에서 챕터패턴으로 바로 가게~~
    ~~예제문장:~~
    ~~## 추신: 오늘은 1회만 연재합니다. 내일은 3회 연재가 있을 예정입니다. 이 에피소드는 아마 내일이면 끝날 것입니다.~~

    ~~## 제134장. 주화입마, 전투력 배가~~

    ~~봉의루 꼭대기, 5층 화청(花廳) 안.~~
    → 2026-09-04에 구현 완료: `ChapterJumpNavigator.breakpoints()`가 다음 챕터 패턴까지 20줄 이하면
    등분하지 않고 곧장 다음 챕터로 건너뛴다.

- TTS 기능 자체가 지금 설정만 있고 TTS가 되게 하는 기능이 없는거 같은데 이거 확인

- **로컬 LLM 기반 TTS**: 기존 Android `TextToSpeech` 대신 로컬에서 돌아가는 LLM(음성합성 모델)을 사용해 더 자연스러운 한국어 발음 제공. 모델 파일을 한 곳이 아니라 여러 소스에서 받을 수 있게 지원 (다운로드 소스 다중화).
