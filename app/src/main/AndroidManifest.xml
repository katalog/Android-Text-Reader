<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- 무료 한글 폰트 다운로드 기능에만 사용 (그 외 모든 기능은 오프라인 동작) -->
    <uses-permission android:name="android.permission.INTERNET" />
    <!-- PC 파일 동기화(.docs/PC_SYNC_SERVER_PLAN.md)의 로컬 서브넷 스캔용 — 현재 연결된 네트워크의
         링크 정보(로컬 IP)를 얻는 데만 쓰고 그 외 트래픽 감청 등은 하지 않음 -->
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <!-- 동기화 페어링 QR 스캔용(.docs/SYNC_MULTIUSER_PLAN.md 스테이지 4) — 선택 기능이라 카메라 없는
         기기도 앱을 설치할 수 있어야 해서 required="false"로 둔다. 권한 거부 시 수동 입력으로 폴백. -->
    <uses-feature android:name="android.hardware.camera" android:required="false" />
    <uses-permission android:name="android.permission.CAMERA" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.TextReader">
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:theme="@style/Theme.TextReader">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>
