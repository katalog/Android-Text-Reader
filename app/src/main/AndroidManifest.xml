<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- Used only for the free Korean font download feature (every other feature works offline) -->
    <uses-permission android:name="android.permission.INTERNET" />
    <!-- For local-subnet scanning in PC file sync (.docs/PC_SYNC_SERVER_PLAN.md) — only used to get
         link info (local IP) for the currently connected network, no other traffic inspection -->
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <!-- For sync-pairing QR scanning (.docs/SYNC_MULTIUSER_PLAN.md stage 4) — optional feature, so
         required="false" lets devices with no camera still install the app. Falls back to manual
         entry if the permission is denied. -->
    <uses-feature android:name="android.hardware.camera" android:required="false" />
    <uses-permission android:name="android.permission.CAMERA" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.TextReader">
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:theme="@style/Theme.TextReader">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>
